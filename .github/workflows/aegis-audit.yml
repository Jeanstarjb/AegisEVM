name: AegisEVM Security Audit

on:
  pull_request:
    paths:
      - '**/*.sol'
  push:
    branches:
      - main
    paths:
      - '**/*.sol'

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install AegisEVM (Neural Engine)
        run: pip install .

      - name: Run AegisEVM Symbolic Execution
        run: |
          echo "🛡️ Starting AegisEVM Analysis on Solidity files..."
          
          # Find all solidity files and run aegis analyze on them
          # If any issues are found, aegisevm returns a non-zero exit code
          # which will automatically fail this GitHub Action and block the PR.
          
          for file in $(find . -name "*.sol"); do
            echo "Analyzing $file..."
            aegis analyze "$file" -t 3 --execution-timeout 60
          done
